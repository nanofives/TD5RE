# ddraw_wrapper module list — SINGLE SOURCE OF TRUTH, shared by build.bat,
# td5mod/Makefile, .github/workflows/build.yml and release.yml (same pattern
# as src/td5re/srcs.txt). One module per line, # comments — avoid writing any
# word ending in ".c" in these comments (Makefile's $(filter %.c,...) picks up
# any such word regardless of line). The last module in the list below needs
# an extra -Wno-unused-function (kept per-build-path, not here).
ddraw_main.c
ddraw4.c
surface4.c
d3d3.c
device3.c
viewport3.c
texture2.c
d3d11_backend.c
png_loader.c
