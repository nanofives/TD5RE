# ddraw_wrapper compile flags — SINGLE SOURCE OF TRUTH, shared by build.bat,
# td5mod/Makefile, .github/workflows/build.yml and release.yml (same pattern
# as src/td5re/cflags.txt). One flag per line, # comments — but note any word
# starting with '-' anywhere in a comment line still gets picked up by the
# .bat/Makefile parsers (they split on whitespace only), so avoid writing
# flag-shaped tokens in these comments. The wrapper debug-log define below is
# now unified across every build path: it was previously missing from CI
# builds while every local build (.bat/Makefile) had it.
-c
-O2
-Wall
-DWIN32
-DWRAPPER_DEBUG
