# Probe: after the StartScreen=44 nav-walk lands on MP TOOLS, move DOWN to
# CONFIRM PROMPT and press ENTER so the shared MP confirm modal shows for a
# screenshot (it must composite OVER the dimmed screen).
# Run with: td5re.exe --SkipIntro=1 --StartScreen=44 --InputScript=td5mod/src/td5re/inputscripts/uiguide_modal_probe.txt

sync menu
560  log mp-guide probe: DOWN to CONFIRM PROMPT + ENTER
560  key down press
590  key enter press
650  log mp-guide probe: modal should be armed now
